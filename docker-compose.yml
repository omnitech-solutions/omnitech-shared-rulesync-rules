services:
  omnitech-shared-mcp:
    build:
      context: ./mcp-server/omnitech-shared-mcp
      dockerfile: Dockerfile
    environment:
      RULESYNC_ROOT: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true
